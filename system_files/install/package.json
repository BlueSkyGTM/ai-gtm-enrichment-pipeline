{
  "name": "ai-gtm-enrichment-pipeline",
  "version": "1.0.0",
  "description": "Three-agent AI enrichment pipeline — Ahab, Nemo, Neptune — over self-hosted Postgres, n8n, and Docker on GCP.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "deploy": "bash deploy.sh"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "google-auth-library": "^9.0.0",
    "node-fetch": "^3.3.2",
    "p-limit": "^5.0.0",
    "pg": "^8.11.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
